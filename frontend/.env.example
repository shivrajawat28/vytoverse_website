# VytoVerse Frontend Environment Variables
# Copy to .env.local for local overrides (not committed).
# Set VITE_API_URL in Netlify dashboard for production.

# Backend API base URL
# Development: /api  (Vite proxy handles forwarding)
# Production:  https://your-app.onrender.com
VITE_API_URL=/api
